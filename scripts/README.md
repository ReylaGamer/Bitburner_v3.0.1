# Independent scripts

This directory is reserved for Bitburner scripts that operate independently of the darkweb system.

Scripts placed here should not be assumed to participate in darkweb startup, deployment, authentication, or worker supervision.

the batcher is the only file that needs to be kicked off manually from the home node.

home/run hwgw-cluster-batcherV4.js
