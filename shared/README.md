# Shared utilities

Place reusable modules and helpers here when they are intentionally shared by more than one independent system.

Keep darkweb-specific behavior in `darkweb/` unless a helper is genuinely shared.
